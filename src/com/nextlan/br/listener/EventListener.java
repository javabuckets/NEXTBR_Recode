package com.nextlan.br.listener;

import org.bukkit.event.Listener;

import com.nextlan.br.NEXTBR;

public class EventListener implements Listener
{
	private NEXTBR plugin;
	
	public EventListener(NEXTBR plugin) 
	{
		this.plugin = plugin;
	}
}